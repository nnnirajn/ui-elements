AOS.init({
  duration: 1000,
    once: true // for making only one time animation
});

   // AOS.init({
   //    offset: 200,
   //    duration: 600,
   //    easing: 'ease-in-sine',
   //    delay: 100,
   //  });