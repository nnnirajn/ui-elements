// Initialize managers
const tooltip = new TooltipManager({
  delay: 0,
  html: true,
  offset: 8,
});

const popover = new PopoverManager({
  html: true,
  offset: 12,
  closeOnClickOutside: true,
});
